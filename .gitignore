.Rproj.user
.Rhistory
.RData
.Ruserdata
.Rdata
.httr-oauth
.DS_Store
.quarto

# Node — node_modules should always live inside site/, never at the repo root.
# A stray "npm install" at the root creates these by accident.
node_modules/

# Instructor-only talking script + its local edit server (kept locally, not published)
examples_talking_script.md
examples_talking_script.html
edit_server.py
Edit talking script (start here).command
_script_backups/
# The one-page print run of show built from the talking script's clock
DA402 Session 2 — Run-Sheet (print to PDF).html
